[pytest]
# The terrain toolkit's offline test suite. Run from tools/:  python -m pytest
testpaths = terrain/tests voxel/tests builder/tests
addopts = -q -p no:cacheprovider
# Windows filesystem mtime granularity can leave stale .pyc files that confuse
# pytest's assertion rewriting; disabling bytecode caching avoids it.
